Build fixes applied:
1. Removed accidental top-level app directory that created a second Next.js app root.
2. Added missing runtime dependencies required by imports:
   - @supabase/ssr
   - @supabase/supabase-js
   - clsx
   - tailwind-merge
3. Removed the mismatched/corrupted package-lock.json so Vercel can generate a fresh lockfile during install.
